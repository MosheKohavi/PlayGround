import { FormUnitConverterDirective } from './form-unit-converter.directive';

describe('UnitConverterDirective', () => {
  it('should create an instance', () => {
    const directive = new FormUnitConverterDirective();
    expect(directive).toBeTruthy();
  });
});
